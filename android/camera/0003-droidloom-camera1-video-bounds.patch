--- a/services/camera/libcameraservice/api1/client2/Parameters.cpp
+++ b/services/camera/libcameraservice/api1/client2/Parameters.cpp
@@ -110,6 +110,15 @@
             maxVideoHeight = h1;
         }
     }
+    // Droidloom's external camera has no hardware video encoder profile. Keep
+    // Camera1 initialization usable by treating the camera preview bounds as
+    // the video bounds when MediaProfiles has no encoder entries.
+    if (maxVideoWidth == 0 || maxVideoHeight == 0) {
+        minVideoWidth = 0;
+        minVideoHeight = 0;
+        maxVideoWidth = MAX_PREVIEW_WIDTH;
+        maxVideoHeight = MAX_PREVIEW_HEIGHT;
+    }
     // These are upper/lower bounds and may not be an actually valid video size
     const Size VIDEO_SIZE_LOWER_BOUND = {minVideoWidth, minVideoHeight};
     Size videoSizeUpperBound = {maxVideoWidth, maxVideoHeight};
