diff -ruN /tmp/camera-patch-check-1009910/camera/device/3.4/default/ExternalCameraDevice.cpp /tmp/tmp.d5xKJLFJtg/camera/device/3.4/default/ExternalCameraDevice.cpp
--- a/camera/device/3.4/default/ExternalCameraDevice.cpp	2026-09-13 14:12:19.944000000 +0800
+++ b/camera/device/3.4/default/ExternalCameraDevice.cpp	2026-09-13 17:11:37.462678232 +0800
@@ -39,8 +39,8 @@
 // Other formats to consider in the future:
 // * V4L2_PIX_FMT_YVU420 (== YV12)
 // * V4L2_PIX_FMT_YVYU (YVYU: can be converted to YV12 or other YUV420_888 formats)
-const std::array<uint32_t, /*size*/ 2> kSupportedFourCCs{
-    {V4L2_PIX_FMT_MJPEG, V4L2_PIX_FMT_Z16}};  // double braces required in C++11
+const std::array<uint32_t, /*size*/ 3> kSupportedFourCCs{
+    {V4L2_PIX_FMT_MJPEG, V4L2_PIX_FMT_Z16, V4L2_PIX_FMT_YUV420}};  // double braces required in C++11
 
 constexpr int MAX_RETRY = 5; // Allow retry v4l2 open failures a few times.
 constexpr int OPEN_RETRY_SLEEP_US = 100000; // 100ms * MAX_RETRY = 0.5 seconds
@@ -274,7 +274,8 @@
     for (const auto& fmt : mSupportedFormats) {
         switch (fmt.fourcc) {
             case V4L2_PIX_FMT_Z16: hasDepth = true; break;
-            case V4L2_PIX_FMT_MJPEG: hasColor = true; break;
+            case V4L2_PIX_FMT_MJPEG:
+            case V4L2_PIX_FMT_YUV420: hasColor = true; break;
             default: ALOGW("%s: Unsupported format found", __FUNCTION__);
         }
     }
@@ -710,6 +711,7 @@
                 hasDepth = true;
                 break;
             case V4L2_PIX_FMT_MJPEG:
+            case V4L2_PIX_FMT_YUV420:
                 hasColor = true;
                 break;
             default:
@@ -731,6 +733,11 @@
                 ANDROID_SCALER_AVAILABLE_STREAM_CONFIGURATIONS_OUTPUT,
                 ANDROID_SCALER_AVAILABLE_STREAM_CONFIGURATIONS,
                 ANDROID_SCALER_AVAILABLE_MIN_FRAME_DURATIONS,
+                ANDROID_SCALER_AVAILABLE_STALL_DURATIONS);
+        initOutputCharskeysByFormat(metadata, V4L2_PIX_FMT_YUV420, halFormats,
+                ANDROID_SCALER_AVAILABLE_STREAM_CONFIGURATIONS_OUTPUT,
+                ANDROID_SCALER_AVAILABLE_STREAM_CONFIGURATIONS,
+                ANDROID_SCALER_AVAILABLE_MIN_FRAME_DURATIONS,
                 ANDROID_SCALER_AVAILABLE_STALL_DURATIONS);
     }
 
diff -ruN /tmp/camera-patch-check-1009910/camera/device/3.4/default/ExternalCameraDeviceSession.cpp /tmp/tmp.d5xKJLFJtg/camera/device/3.4/default/ExternalCameraDeviceSession.cpp
--- a/camera/device/3.4/default/ExternalCameraDeviceSession.cpp	2026-09-13 14:12:19.945000000 +0800
+++ b/camera/device/3.4/default/ExternalCameraDeviceSession.cpp	2026-09-13 17:11:37.463027508 +0800
@@ -1438,7 +1438,8 @@
         return false;
     };
 
-    if (req->frameIn->mFourcc != V4L2_PIX_FMT_MJPEG && req->frameIn->mFourcc != V4L2_PIX_FMT_Z16) {
+    if (req->frameIn->mFourcc != V4L2_PIX_FMT_MJPEG &&
+            req->frameIn->mFourcc != V4L2_PIX_FMT_YUV420 && req->frameIn->mFourcc != V4L2_PIX_FMT_Z16) {
         return onDeviceError("%s: do not support V4L2 format %c%c%c%c", __FUNCTION__,
                 req->frameIn->mFourcc & 0xFF,
                 (req->frameIn->mFourcc >> 8) & 0xFF,
@@ -1487,8 +1488,9 @@
     }
 
     // TODO: in some special case maybe we can decode jpg directly to gralloc output?
-    if (req->frameIn->mFourcc == V4L2_PIX_FMT_MJPEG) {
-        ATRACE_BEGIN("MJPGtoI420");
+    if (req->frameIn->mFourcc == V4L2_PIX_FMT_MJPEG ||
+            req->frameIn->mFourcc == V4L2_PIX_FMT_YUV420) {
+        ATRACE_BEGIN("V4L2toI420");
         int res = 0;
         if (mCameraMuted) {
             res = libyuv::ConvertToI420(
@@ -1498,6 +1500,21 @@
                     static_cast<uint8_t*>(mYu12FrameLayout.cr), mYu12FrameLayout.cStride, 0, 0,
                     mYu12Frame->mWidth, mYu12Frame->mHeight, mYu12Frame->mWidth,
                     mYu12Frame->mHeight, libyuv::kRotate0, libyuv::FOURCC_RAW);
+        } else if (req->frameIn->mFourcc == V4L2_PIX_FMT_YUV420) {
+            const size_t ySize = static_cast<size_t>(mYu12Frame->mWidth) * mYu12Frame->mHeight;
+            const size_t expected = ySize + ySize / 2;
+            if (inDataSize < expected) {
+                res = -EINVAL;
+            } else {
+                res = libyuv::I420Copy(
+                    inData, mYu12Frame->mWidth,
+                    inData + ySize, mYu12Frame->mWidth / 2,
+                    inData + ySize + ySize / 4, mYu12Frame->mWidth / 2,
+                    static_cast<uint8_t*>(mYu12FrameLayout.y), mYu12FrameLayout.yStride,
+                    static_cast<uint8_t*>(mYu12FrameLayout.cb), mYu12FrameLayout.cStride,
+                    static_cast<uint8_t*>(mYu12FrameLayout.cr), mYu12FrameLayout.cStride,
+                    mYu12Frame->mWidth, mYu12Frame->mHeight);
+            }
         } else {
             res = libyuv::MJPGToI420(
                     inData, inDataSize, static_cast<uint8_t*>(mYu12FrameLayout.y),
